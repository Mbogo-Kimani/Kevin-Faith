// Optional: You can add JavaScript here for additional functionality if needed.
// For now, the carousel animation is done using CSS, but you could add additional interactivity here.

document.addEventListener('DOMContentLoaded', function () {
    console.log("Wedding website is ready!");
});
